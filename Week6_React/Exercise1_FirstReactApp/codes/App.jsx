import React from "react";         //imports React library to use JSX
function App(){      //App: a functional component
  return(
    <h1>Welcome to the first session of React</h1>          //the component renders this; it's JSX
  )
}

export default App;    //exporting App componeny so other files can use it
