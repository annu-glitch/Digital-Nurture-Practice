import React, {Component} from 'react';

export class Contact extends Component{   //named export; extends base class Component
    render(){ //method that returns what to display in UI
        return( //returns JSX
            <div>    
                <h3>Welcome to the Contact Page of Student Management Portal</h3>
            </div>
        );
    }
}
